alice:
  email: "alice@example.com"
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
  auth_level: 'admin'
  token: 'alice_token'
alice_without_token:
  email: "alice_without_token@example.com"
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
snow:
  email: "snow@example.com"
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
  auth_level: 'user'
  token: 'snow_token'
snow_without_token:
  email: "snow_without_token@example.com"
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
  auth_level: 'user'